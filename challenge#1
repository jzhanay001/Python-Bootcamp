def sum1(list):
    A=len(list)
    if A != 0:
        b=sum(list)
        return b
    else:
        b=sum(list)
        return b

input_list=sum1([1,3,5,7]) # input numbers here int or float
print (input_list)
