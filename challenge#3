def evens(n):
    N=abs(n)
    if type(N) == type(50):
        answer=N*2
        return answer
    else:
        return
d=evens(19)  # input numbers here, only accept int number
print (d)       # other type will return None
